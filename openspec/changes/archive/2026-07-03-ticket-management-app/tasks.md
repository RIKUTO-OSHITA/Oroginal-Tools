## 1. プロジェクトセットアップ

- [x] 1.1 Vite + React + TypeScriptでプロジェクトをscaffoldする
- [x] 1.2 React Routerを依存関係に追加する
- [x] 1.3 ディレクトリ構成(components/pages/store等)を作成する
- [x] 1.4 チケットの型定義(`Ticket`: id, title, purpose, acceptanceCriteria, notes, status, createdAt, updatedAt)を作成する

## 2. データ永続化層

- [x] 2.1 `localStorage`ラッパー(`ticketStore`)を実装する(読み込み/保存の共通処理)
- [x] 2.2 チケット一覧の取得・保存機能を実装する
- [x] 2.3 `Tickets-N`形式のID採番カウンタを`localStorage`に保存・読み込みする機能を実装する(削除してもIDを再利用しない)
- [x] 2.4 チケットの作成(create)・更新(update)・削除(delete)・ID指定取得(getById)の各関数を実装する

## 3. 状態管理

- [x] 3.1 `TicketsProvider`(Context)を実装し、アプリ起動時に`localStorage`からチケット一覧を読み込む
- [x] 3.2 `useTickets()`フックを実装し、一覧取得・作成・更新・削除・ID指定取得をコンポーネントに提供する
- [x] 3.3 CRUD操作のたびに`localStorage`へ変更を同期する

## 4. ルーティング

- [x] 4.1 `HashRouter`を導入し、`/`(ボード)、`/tickets/new`(新規作成)、`/tickets/:ticketId`(詳細/編集)、`/completed`(完了一覧)のルートを定義する
- [x] 4.2 存在しない`ticketId`にアクセスした場合の「not found」表示を実装する

## 5. チケット作成・編集・削除

- [x] 5.1 チケット作成フォーム(チケット名・目的・受け入れ条件・補足の入力)を実装する
- [x] 5.2 チケット名の必須バリデーションを実装する
- [x] 5.3 作成成功時にボード(`/`)へ遷移する処理を実装する
- [x] 5.4 チケット編集フォーム(既存値の初期表示・更新)を実装する
- [x] 5.5 ステータス変更UI(To Do / In Progress / Done)を編集画面に実装する
- [x] 5.6 削除ボタンと確認ダイアログを実装し、確認後にのみ削除される処理を実装する

## 6. ボード画面

- [x] 6.1 ステータスが"To Do"/"In Progress"のチケットのみを列表示するボードUIを実装する
- [x] 6.2 各チケットカードにID(`Tickets-N`)と名前を表示する
- [x] 6.3 チケットカードのクリックで詳細ページ(パーマリンク)へ遷移する処理を実装する

## 7. チケット詳細/パーマリンク画面

- [x] 7.1 URLの`ticketId`パラメータからチケットを取得し詳細を表示する処理を実装する
- [x] 7.2 ID、チケット名、目的、受け入れ条件、補足、ステータスを表示するレイアウトを実装する
- [x] 7.3 リロード時にも同じチケット詳細が表示されることを確認する

## 8. 完了チケットアーカイブ画面

- [x] 8.1 ステータスが"Done"のチケットのみを一覧表示するページを実装する
- [x] 8.2 一覧の各行からチケット詳細ページへ遷移できるようにする
- [x] 8.3 ステータスをDoneに変更した直後、ボードから消えてこのページに表示されることを確認する

## 9. 動作確認

- [x] 9.1 チケットの作成・編集・削除が正しく動作することを手動確認する
- [x] 9.2 チケットIDが`Tickets-1`から連番で採番され、削除してもIDが再利用されないことを確認する
- [x] 9.3 チケット詳細URLへ直接アクセス・リロードしても同じ内容が表示されることを確認する
- [x] 9.4 ブラウザを再起動してもチケットデータが保持されていることを確認する
- [x] 9.5 完了(Done)にしたチケットがボードから消え、完了ページに表示されることを確認する
